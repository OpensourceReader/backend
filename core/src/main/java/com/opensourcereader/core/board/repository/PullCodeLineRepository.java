package com.opensourcereader.core.board.repository;

import com.opensourcereader.core.board.entity.PullCodeLine;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PullCodeLineRepository extends JpaRepository<PullCodeLine, Long> {

}
