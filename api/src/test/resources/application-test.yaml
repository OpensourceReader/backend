spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true
jwt:
  secretKey: ${JWT_SECRET_KEY}
  access-token-expiration: 600 # 10 분
  refresh-token-expiration: 604800 # 7일

logging:
  level:
    root: info
