spring:
  jpa:
    hibernate:
      ddl-auto: validate
    properties:
      hibernate:
        format_sql: true
        show_sql: true

github:
  personal-token: "test-token"

jwt:
  secretKey: "test123123123"
  access-token-expiration: 300
  refresh-token-expiration: 600

logging:
  level:
    root: info
